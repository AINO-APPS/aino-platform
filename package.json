{
  "name": "aino-platform",
  "version": "1.0.0",
  "private": true,
  "description": "AINO platform — web, desktop, backend, contracts, and infrastructure",
  "scripts": {
    "release": "pwsh ./scripts/release.ps1",
    "check:generated": "node scripts/check-no-graphify-tracked.mjs",
    "check:stateless": "node scripts/check-statelessness.mjs",
    "check:sql-routes": "node scripts/check-no-sql-in-routes.mjs",
    "check:file-sizes": "node scripts/check-server-file-sizes.mjs",
    "check:client-file-sizes": "node scripts/check-client-file-sizes.mjs",
    "check:desktop-file-sizes": "node scripts/check-desktop-file-sizes.mjs",
    "check:desktop-ipc": "node scripts/check-desktop-ipc.mjs",
    "check:module-readmes": "node scripts/check-module-readmes.mjs",
    "check:repo-independence": "node scripts/check-repo-independence.mjs",
    "contracts:validate": "node scripts/validate-contracts.mjs && node --test contracts/tests/*.test.mjs",
    "check:railway": "node scripts/verify-railway-config.mjs",
    "check:guardrails": "npm run check:generated && npm run check:railway && npm run check:stateless && npm run check:sql-routes && npm run check:file-sizes && npm run check:client-file-sizes && npm run check:desktop-file-sizes && npm run check:desktop-ipc && npm run check:module-readmes && npm run check:repo-independence"
  },
  "devDependencies": {
    "railway": "^3.11.0"
  }
}
